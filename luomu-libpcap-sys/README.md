# luomu-libpcap-sys

Unsafe Rust `libpcap` bindings generated with
[bindgen](https://github.com/rust-lang/rust-bindgen).

# Compilation in Alpine (e.g. Rust's Alpine container)

Install depencies before attempting to compile this crate:

```console
# apk update
# apk build-base linux-headers flex bison
```
